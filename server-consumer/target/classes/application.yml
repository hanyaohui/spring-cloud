server:
  port: 8082

spring:
  application:
    name: server-consumer

eureka:
  client:
    service-url:
      defaultZone: http://eurekaPeer1:8000/eureka/

feign:
  hystrix:
    enabled: true #开启熔断机制
